import type { ExpoConfig } from 'expo/config';

// Placeholder Expo app configuration — no screens/business logic.
const config: ExpoConfig = {
  name: 'HMS',
  slug: 'hms-mobile',
  version: '0.1.0',
  orientation: 'portrait',
  userInterfaceStyle: 'automatic',
  scheme: 'hms',
};

export default config;
