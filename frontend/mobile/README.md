# HMS Mobile

React Native mobile application for the Hospital Management System.

_Placeholder — implementation to follow. `android/` and `ios/` will be populated by the React Native CLI when the app is initialized; they are kept as tracked empty folders until then. See [docs/FolderStructure.md](../../docs/FolderStructure.md) for the intended layout of this app._
