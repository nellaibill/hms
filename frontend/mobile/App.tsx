// Expo's managed-workflow entry point looks for a root App component. The real
// bootstrap lives in src/app/App.tsx, matching docs/FrontendArchitecture.md's folder
// layout — this file only re-exports it.
export { App as default } from './src/app/App';
