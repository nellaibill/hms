# HMS Shared

Cross-platform TypeScript code shared between the HMS web and mobile apps: `types`, `api-client`, `validation`, `constants`, and `utils`.

_Placeholder — implementation to follow. See [docs/FolderStructure.md](../../docs/FolderStructure.md) for what belongs in each subfolder._
