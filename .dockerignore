# .NET
**/bin/
**/obj/
**/.vs/

# Node — mobile isn't containerized. node_modules/dist are rebuilt fresh inside the
# frontend image (host-built artifacts could be Windows-native or stale) rather than
# copied in, so excluding them here doesn't affect what actually ships.
frontend/mobile/
**/node_modules/
**/dist/
**/build/
**/coverage/

# VCS / tooling
.git/
.github/
.claude/
docs/

# Env / secrets — never baked into an image
.env
.env.local
.env.*.local
